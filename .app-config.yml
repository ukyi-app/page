kind: service
resources:
  requests: { cpu: 50m, memory: 96Mi }
  limits: { cpu: 300m, memory: 256Mi }
route:
  public: true
  host: page.ukyi.app
  paths: ["/"]
probes:
  liveness: { path: /healthz }
  readiness: { path: /readyz }
deploy:
  autoDeploy: true
