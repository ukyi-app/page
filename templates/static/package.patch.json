{
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "test": "vite build",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "vite": "^8.1.0"
  }
}
